<h1>New Category</h1>

<%= form_for [:admin, @taxon] do |f| %>
  <%= f.label :name %>
  <%= f.text_field :name %>

  <%= f.label :description %>
  <%= f.text_area :description %>

  <%= f.submit "Create Category" %>
<% end %>